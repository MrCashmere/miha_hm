# 鸿米家 1.1.3-OS 更新说明

> 版本号：**1.1.3-OS**　Version Code：**1000130**
>
> ⚠️ **本版尚未发布。** 改动已经在 `main` 分支上，但版本号与安装包要等正式发布时再一并更新。

本次只修一个问题 —— 也是从 1.1.0 起一直挂在「已知问题」里的那一个。

---

## 问题修复

### 修复切换标签页后列表不回顶部

在标签页之间切换时，列表会停在上次浏览的位置，而不是回到顶部。
这个问题从 1.1.0 起一直存在，长期只能靠首页顶栏的「列表回正」按钮手动处理。

现已修复：**切换标签页后，该页列表会自动回到顶部**，不再需要手动干预。

---

## 说明

### 标签栏高亮的更新时机

修复过程中调整了标签栏高亮的刷新时机：它现在**在切换动画结束后**更新，
而不是在动画开始时就切换到新标签。

如果开着「首页平滑切换」（300 毫秒动画），高亮会随之滞后约 300 毫秒。
把该项关闭则没有这个现象。
